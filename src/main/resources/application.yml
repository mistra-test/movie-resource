spring:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  application:
    name: movie-resource
  datasource:
    url: jdbc:h2:mem:testdb

logging:
  level:
    "[com.netflix.discovery]": OFF

server:
  port: 8781
